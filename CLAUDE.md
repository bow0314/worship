# CLAUDE.md

本專案是教會敬拜歌詞頁的靜態 HTML 集合，整體說明見 README.md。以下是額外的內容慣例。

## 詩歌編號格式

- 只有「詩歌」本身編號；經文（宣召）、信息、分享時間、代禱、結束禱告等程序項目一律不編號、不顯示編號圖示。
- 編號格式固定為：半形括弧 + 空格 + 數字 + 空格 + 半形括弧 + 空格 + 歌名，例如「( 1 ) 愛使我們相聚一起」。
- 編號只計「詩歌」，中間穿插的程序項目不佔號碼（例如順序是「宣召、詩歌A、信息、詩歌B」，詩歌A是 ( 1 )、詩歌B是 ( 2 )）。
- 這組「( n ) 歌名」文字要同時套用在畫面上的歌曲標題，以及浮動歌詞提示面板顯示的名稱。

## 浮動順序面板（演唱順序代碼）

- 格式沿用 7/19、8/16 頁面的慣例，不是 8/23 的寫法：段落之間用全形「–」（en dash）分隔，例如「V–C1–V–C2–V」，不要寫成 8/23 那種空白或字母數字連在一起的簡寫（如「VC1C2」）。
- 面板顯示時，短橫線要降到 10% 透明度（`.of-dash`），字母後面的數字（V1、C2…）要縮小並依數字上色（`.of-num-1`／`.of-num-2`／`.of-num-3`），不能直接把 `data-order` 字串原樣塞進 `textContent`。
- 尾句用「*尾」標記，重複用「×N」（例如「*尾×2」、「B×4」）。
- 之後每次做這種帶「浮動順序面板」的敬拜頁，都要照這個格式處理。

## 工作慣例

- 使用者說「發布」，指的是把目前分支合併進 `main`（開 PR 後 merge），不是其他意思。
- 對本專案內容（歌詞頁等）做的異動，不用等使用者說「發布」才處理——做完就直接 commit、push、開 PR 並合併進 main，不用先詢問確認。
